import { Component } from '@angular/core';

@Component({
  // The root element doesn't need a selector (and if you provide one it will be removed anyway)

  // As this is a opentui platform that doesn't actually do anything, I'm not providing a template.
  template: ''
})
export class App {
}
